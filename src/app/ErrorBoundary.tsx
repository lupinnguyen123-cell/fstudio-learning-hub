import { Component, type ErrorInfo, type ReactNode } from 'react'
interface Props { children: ReactNode } interface State { hasError: boolean }
export class ErrorBoundary extends Component<Props, State> { state: State = { hasError: false }; static getDerivedStateFromError(): State { return { hasError: true } } componentDidCatch(error: Error, info: ErrorInfo) { console.error('Application error', error, info) } render() { if (this.state.hasError) return <main className="system-page"><span className="error-code">Oops</span><h1>Đã xảy ra lỗi</h1><p>Vui lòng tải lại trang để tiếp tục.</p><button className="button button-primary" onClick={() => window.location.reload()}>Tải lại trang</button></main>; return this.props.children } }
