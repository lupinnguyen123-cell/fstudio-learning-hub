import { ArrowLeft, ArrowRight } from 'lucide-react'
export function QuizNavigation({ current, total, answered, onGo, onSubmit }: { current: number; total: number; answered: boolean[]; onGo: (index: number) => void; onSubmit: () => void }) {
  return <div className="quiz-navigation"><div className="question-index" aria-label="Danh sách câu hỏi">{answered.map((isAnswered, index) => <button type="button" key={index} className={`${current === index ? 'current' : ''} ${isAnswered ? 'answered' : ''}`} aria-label={`Câu ${index + 1}${isAnswered ? ', đã trả lời' : ', chưa trả lời'}`} onClick={() => onGo(index)}>{index + 1}</button>)}</div><div className="quiz-nav-actions"><button type="button" className="button button-secondary" disabled={current === 0} onClick={() => onGo(current - 1)}><ArrowLeft />Câu trước</button>{current < total - 1 ? <button type="button" className="button button-primary" onClick={() => onGo(current + 1)}>Câu sau<ArrowRight /></button> : <button type="button" className="button button-primary" onClick={onSubmit}>Nộp bài</button>}</div></div>
}
